<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_gradient"
    android:orientation="vertical"
    tools:context="com.dpagliotto.gimb.view.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/content_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        android:visibility="invisible" />

    <FrameLayout
        android:id="@+id/contentFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/content_toolbar"
        android:layout_margin="15dp"
        android:layout_marginBottom="5dp">
    </FrameLayout>

    <RelativeLayout
        android:id="@+id/layoutProgress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="#dc000000"
        android:visibility="invisible">

        <ProgressBar
            android:id="@+id/pbMainProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:theme="@style/AppTheme.Progress"
            android:layout_centerInParent="true" />

        <com.dpagliotto.gimb.components.CustomTextViewGIMBTitle
            android:id="@+id/txtMainProgressMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/pbMainProgress"
            android:layout_centerVertical="true"
            android:gravity="center"
            android:lines="3"
            android:textAlignment="center" />

    </RelativeLayout>

</RelativeLayout>
